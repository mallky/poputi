{
  "name": "poputi-server",
  "version": "1.0.0",
  "description": "WebSocket server for Poputi app",
  "main": "websocket/server.ts",
  "scripts": {
    "start": "ts-node websocket/server.ts",
    "dev": "nodemon --exec ts-node websocket/server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "nodemon": "^3.0.2"
  }
}
